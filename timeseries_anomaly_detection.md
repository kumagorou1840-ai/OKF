---
type: ナレッジ仕様書
title: 時系列異常検知と障害原因特定ガイドライン
category: データサイエンス / 時系列解析 / 異常検知
tags: [TimeSeries, AnomalyDetection, ZScore, 3Sigma, Python, Pandas, Matplotlib, OKF]
created_at: 2026-07-27
updated_at: 2026-07-27
---

# 時系列異常検知と障害原因特定ガイドライン (OKF完全版)

---

## 📋 1. 前提情報 (選択能力・メタデータ)

| 項目 | 内容 |
| :--- | :--- |
| **領域** | データサイエンス・時系列解析・システム障害監視 |
| **タイトル** | 時系列異常検知と障害原因特定ガイドライン |
| **できること** | 売上・トラフィック・経費データ等の時系列推移から、統計的手法（3シグマ法 / Zスコア）を用いて突発スパイクやアクセス急減などの異常値を検知し、多角的な原因レポートと緊急調査アクションを出力 |
| **実例** | 過去2週間の平均5,000アクセス/時から本日10時以降1,200〜1,350アクセス/時への急落を検知（しきい値3,522.58を下回る異常値判定）、システム障害・デプロイミス・計測エラーなど5大原因を特定 |
| **リソース** | Python (`pandas`, `numpy`, `matplotlib`) |
| **関連ドキュメント** | [モンテカルロシミュレーション](file:///C:/Users/PC_User/OKF/monte_carlo_simulation.md) \| [多変量解析自動実行](file:///C:/Users/PC_User/OKF/multivariate_analysis.md) \| [OKF仕様・運用ガイド](file:///C:/Users/PC_User/OKF/okf_specification_overview.md) |
| **全体目次** | [OKFナレッジインデックス (INDEX.md)](file:///C:/Users/PC_User/OKF/INDEX.md) |

---

## 🔬 2. よっちゃん流ハイブリッド診断 (分析＆未来ベクトル)

### 🧠 Karpathy流「8つの視点」によるシステム評価

1. **Software 1.0 / 2.0 / 3.0 の共存**:
   - **1.0**: 3シグマ法 ($Z = \frac{X - \mu}{\sigma}$) や固定しきい値による統計的計算式。
   - **2.0**: `pandas` / `numpy` による時系列ローリングウィンドウ演算と異常フラグ自動付与。
   - **3.0**: 異常値検知後、AIエージェント（Antigravity）が「インフラ障害」「デプロイミス」「計測タグ脱落」「広告停止」などの要因ログと照合し、優先度付き原因調査レポートを自動生成。
2. **LLM as an OS**:
   - Webアクセスやインフラの監視メトリクスをAIエージェントが「OSの診断監視サブシステム」として常時モニタリング。
3. **Partial Autonomy (部分的自律性 / スライダー)**:
   - 3シグマによる異常検知と要因候補抽出は完全自動化、緊急度の高い「サーバー復旧やロールバック決定」はHuman-in-the-loop（人間のエンジニア）へ委任。
4. **Iron Man Suit (アイアンマンスーツ / 認知拡張)**:
   - 数十種類のサーバーログやGA計測値を人間が1つずつ探す手間を省き、システム異常と原因候補を一瞬で提示する監視能力の拡張。
5. **Vibe Coding (意図のトランスパイル)**:
   - 「アクセスが落ちておかしい」という直感的な違和感（Vibe）に対し、決定論的な統計しきい値判定と可視化グラフを出力。
6. **LLM Psychology (心理学 / 確信度)**:
   - 「一時的なアクセス減少か、システム障害か」というエンジニアの迷いに対し、過去データ比較と下限しきい値（3522.58）に基づく客観的根拠を提供。
7. **Compilation Analogy (コンパイル)**:
   - 生の時系列ログデータ $\rightarrow$ 統計推移パラメータ $\rightarrow$ 異常フラグ判定 $\rightarrow$ アクションプランへのコンパイル。
8. **Build for Agents**:
   - トラフィック異常検知だけでなく、経費スパイク検知や不正アクセス検出等へも即座に再利用できるコード設計。

---

### 🧩 内部構造の診断 (核・質・特・欠)

- **核 (核心)**:
  - 過去データ（ベースライン）の平均 $\mu$ と標準偏差 $\sigma$ に基づく下限しきい値（$\mu - 3\sigma$）の設定。
- **質 (品質)**:
  - 短時間の突発変動に惑わされず、統計的に明確な有意差（3シグマ離脱）のみを精度高く捕捉。
- **特 (特性)**:
  - 検知後の「次のアクション（5大原因分類と緊急調査手順）」までが一体系化された実用性。
- **欠 (欠損・課題)**:
  - 曜日・時間帯・季節性（Seasonality）による周期的変動に対応するため、今後は STL分解や Prophet などの季節調整モデルの拡張が有効。

---

### 🚀 未来ベクトル (方・移・拡・新)

- **方 (方向性)**:
  - 静的3シグマ法から、季節性を考慮した移動窓（Rolling Window）付き動的しきい値検知への深化。
- **移 (移動性)**:
  - Grafana / Prometheus、Datadog、CloudWatch、GCP Cloud Monitoring、Python CLI 等どこへでも組み込み可能。
- **拡 (拡張性)**:
  - Slack / Teams への緊急アラート通知フックや、関連ログ（Nginx error.log、DNS設定変更履歴）の自動検索連携。
- **新 (新規性)**:
  - 単に異常を知らせるだけのアラームから、AIが原因可能性の仮説ランキングと対応手順までを併せて提示する「自律対応型監視」の確立。

---

## 💬 3. 対話スクリプト & タイムライン

### ⏱️ タイムライン
| タイムスタンプ | セクション名 | 主な内容 |
| :--- | :--- | :--- |
| **00:00** | トラフィック急落データの提示 | 過去平均5000アクセスに対し、本日10時以降1200〜1350へと急減したデータの入力。 |
| **02:15** | 3シグマ法・Zスコア判定ロジック | 過去データ（ベースライン）からの統計的乖離度合いと判定式の解説。 |
| **05:30** | Python による異常検知実装 | `pandas` を用いた下限しきい値算出と判定・可視化スクリプトの実行。 |
| **09:00** | 5大原因の多角的分析 | インフラ障害、デプロイ変更、流入源変化、計測ツール不具合、外部要因の分類。 |
| **13:00** | アクションプランの提示 | 技術的緊急確認、変更履歴照会、Web解析ツールの3段階調査手順。 |

---

### 🗣️ 対話文字起こしログ

> **👤 ユーザー (よっちゃん)**:  
> 「過去2週間の平均時間別Webサイトアクセス数は約5000でしたが、本日午前10時から以下のデータのようにアクセス数が異常に低下しています。これは異常値として検知可能でしょうか？もし可能であれば、この低下の主な原因として考えられるものを複数提示してください。  
>  
> 時間帯,アクセス数  
> 本日9時,4800  
> 本日10時,1200  
> 本日11時,1150  
> 本日12時,1300  
> 本日13時,1250  
> 本日14時,1350」  
>  
> ---  
>  
> **🤖 Antigravity OS**:  
> 「はい、提示されたデータは**時系列異常検知の典型的なケースであり、異常値として非常に高い確度で検知可能**です。  
> 過去2週間の平均が約5000アクセス/時であるのに対し、本日午前10時から1200〜1350アクセス/時という大幅な低下は、統計的に有意な変化であると判断できます。」  

---

## 💻 4. 実装コード (Python & Pandas / Matplotlib)

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

# --- 1. 過去データのシミュレーション (ベースラインの確立) ---
# 過去2週間 (14日間) の午前9時から午後2時までの時間別アクセス数をシミュレート
# 平均: 5000, 標準偏差: 500 と仮定
np.random.seed(42)
past_data = pd.DataFrame({
    'timestamp': pd.to_datetime(pd.date_range(start='2026-07-01 09:00', periods=14*6, freq='h')),
    'access_count': np.random.normal(loc=5000, scale=500, size=14*6).astype(int)
})

# 全体の平均と標準偏差を計算
mu_past = past_data['access_count'].mean()
sigma_past = past_data['access_count'].std()

print(f"過去データの平均アクセス数: {mu_past:.2f}")
print(f"過去データの標準偏差: {sigma_past:.2f}")

# --- 2. しきい値の設定 (3シグマ法) ---
# 下限しきい値を設定 (平均 - 3 * 標準偏差)
lower_bound = mu_past - (3 * sigma_past)
print(f"異常検知の下限しきい値 (平均 - 3σ): {lower_bound:.2f}\n")

# --- 3. 本日のデータ ---
today_data_raw = {
    '時間帯': ['本日9時', '本日10時', '本日11時', '本日12時', '本日13時', '本日14時'],
    'アクセス数': [4800, 1200, 1150, 1300, 1250, 1350]
}
df_today = pd.DataFrame(today_data_raw)

# --- 4. 異常検知ロジック ---
df_today['is_anomaly'] = df_today['アクセス数'] < lower_bound

print("--- 本日のアクセス数と異常検知結果 ---")
print(df_today)
print("\n--- 検知された異常 ---")
anomalies = df_today[df_today['is_anomaly']]
if not anomalies.empty:
    for index, row in anomalies.iterrows():
        print(f"時刻: {row['時間帯']}, アクセス数: {row['アクセス数']} (しきい値 {lower_bound:.2f} を下回りました)")
else:
    print("異常は検知されませんでした。")

# --- 5. 可視化 ---
plt.figure(figsize=(10, 5))
plt.plot(df_today['時間帯'], df_today['アクセス数'], marker='o', label='本日アクセス数', color='#3182CE', linewidth=2)
plt.axhline(y=mu_past, color='#38A169', linestyle='--', label=f'過去平均 ({mu_past:.0f})')
plt.axhline(y=lower_bound, color='#E53E3E', linestyle='--', label=f'異常検知下限 3σ ({lower_bound:.0f})')
plt.scatter(anomalies['時間帯'], anomalies['アクセス数'], color='#E53E3E', s=120, zorder=5, label='異常判定ノード')

plt.title('Web Site Access Count Anomaly Detection (3-Sigma Method)')
plt.xlabel('Time Range')
plt.ylabel('Access Count')
plt.grid(True, linestyle=':', alpha=0.6)
plt.legend()
plt.tight_layout()
plt.show()
```

---

### 📊 実行結果数値例

```text
過去データの平均アクセス数: 5013.88
過去データの標準偏差: 497.10
異常検知の下限しきい値 (平均 - 3σ): 3522.58

--- 本日のアクセス数と異常検知結果 ---
    時間帯  アクセス数  is_anomaly
0   本日9時     4800       False
1  本日10時     1200        True
2  本日11時     1150        True
3  本日12時     1300        True
4  本日13時     1250        True
5  本日14時     1350        True

--- 検知された異常 ---
時刻: 本日10時, アクセス数: 1200 (しきい値 3522.58 を下回りました)
時刻: 本日11時, アクセス数: 1150 (しきい値 3522.58 を下回りました)
時刻: 本日12時, アクセス数: 1300 (しきい値 3522.58 を下回りました)
時刻: 本日13時, アクセス数: 1250 (しきい値 3522.58 を下回りました)
時刻: 本日14時, アクセス数: 1350 (しきい値 3522.58 を下回りました)
```

---

## 🔍 5. 低下の主な要因と緊急対応アクションプラン

### 1. 5大原因の分類
1. **システム・インフラ障害**: Webサーバーダウン・DB過負荷・DNS/CDNトラブル・ロードバランサー不具合。
2. **デプロイ・設定変更**: 直前のプログラムリリースでのバグ、Nginx/Apache設定ミス、ファイアウォール誤ブロック。
3. **トラフィック流入源の切断**: 広告キャンペーン停止、検索順位急落、外部リンク切れ。
4. **計測ツールの問題**: Google Analytics タグの剥がれ、タグマネージャーの設定ミス。
5. **外部障害**: 地域的インターネット障害、特定ISPの通信障害。

### 2. 緊急対応 3 ステップ
* **Step 1 (即時)**: `curl` やブラウザ実機テストでサイト応答（200 OK / 500 Error）を確認、サーバーログ (`error.log`) を確認。
* **Step 2 (15分以内)**: 本日9時〜10時の間のデプロイ履歴・DNS/CDN設定変更履歴を調査。
* **Step 3 (30分以内)**: Google Analytics のリアルタイムレポートでデバイス別・地域別の偏りがないか診断。

---

## 🛠️ 6. 運用 & 検証ガイドライン

### ローカルでの再現検証実行
以下の Python ワンライナーで3シグマ判定が即座に動作することを確認できます。

```bash
python -c "
import numpy as np
mu, sigma = 5000, 500
lower = mu - 3 * sigma
today_val = 1200
print(f'Lower Bound: {lower}, Today 10am: {today_val}, Is Anomaly: {today_val < lower}')
"
```

---

* [OKFナレッジインデックスに戻る](file:///C:/Users/PC_User/OKF/INDEX.md)
